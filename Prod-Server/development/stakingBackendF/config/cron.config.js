module.exports = {
    // Production schedules
    DAILY_RETURNS_SCHEDULE: '0 0 * * *' // Runs at midnight every day
    // rewardCron: '0 0 * * 0',   // Every Sunday at midnight
    
    // Development/testing schedules (commented out by default)
    // stakingCron: '* * * * *',  // Every minute
    // rewardCron: '*/5 * * * *'  // Every 5 minutes
};